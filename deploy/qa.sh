# run build script with prod settings

# save image
docker save --output saved-image.tar my-image:1.0.0

# transfer image
scp

# ssh
copy secrets
docker load --input saved-image.tar
docker run mounting secrets
